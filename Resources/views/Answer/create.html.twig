{% extends 'QcmAdminBundle::layout.html.twig' %}

{% block title %}{{ 'qcm_admin.answer.add'|trans }}{% endblock %}

{% block content %}
    {{ actions.title('qcm_admin.answer.add'|trans, 'glyphicon-check') }}

    <form class="form-horizontal col-sm-12 text-center" novalidate action="{{ path('qcm_admin_answer_add') }}" {{ form_enctype(form) }} method="POST">
        {{ form_widget(form) }}
        {{ actions.cancel(path('qcm_admin_answer_list')) }}
        {{ actions.submit() }}
    </form>
{% endblock %}
